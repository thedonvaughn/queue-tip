source :gemcutter
gem "rails", "~> 2.3.11"
gem "sqlite3"

gem 'fastercsv'
gem 'adhearsion', '0.8.4', :git => 'https://github.com/adhearsion/adhearsion.git', :tag => '0.8.4'
gem 'mysql', :require => false  # adhearsion uses it

# bundler requires these gems in all environments
# gem "nokogiri", "1.4.2"
# gem "geokit"

group :development do
  # bundler requires these gems in development
  # gem "rails-footnotes"
end

group :test do
  # bundler requires these gems while running tests
  # gem "rspec"
  # gem "faker"
end

